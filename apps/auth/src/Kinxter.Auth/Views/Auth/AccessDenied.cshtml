@{
    var text = Kinxter.Auth.Rendering.AuthUiText.For(ViewData["Locale"] as string);
    Layout = "/Views/Auth/_Layout.cshtml";
    ViewData["Title"] = text["Access denied"];
}
<p class="eyebrow">@text["Account security"]</p>
<h1>@text["Access denied"]</h1>
<p class="lead">@text["Your account cannot access this resource."]</p>
